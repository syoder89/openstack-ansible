---

component_skel:
  zookeeper:
    belongs_to:
      - zookeeper_all

container_skel:
  zookeeper_container:
    belongs_to:
      - midonet-nsdb_containers
    contains:
      - zookeeper
    properties:
      service_name: zookeeper
      container_release: trusty

